version: 1.0
runtime: python3.10

endpoints:
  - name: REST API
    port: 8000
    type: REST
    networkVisibility: Public
    context: /

build:
  commands:
    - pip install -r requirements.txt

run:
  command: gunicorn MitramBackend.wsgi:application --bind 0.0.0.0:8000
